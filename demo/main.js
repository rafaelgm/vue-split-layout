import { createApp } from 'vue'
import App from './App.vue'
import './App.scss' // Import global styles for demo

// Create the Vue 3 app instance
const app = createApp(App);

// Mount the app to the DOM
app.mount('#app');
